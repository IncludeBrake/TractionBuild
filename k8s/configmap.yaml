apiVersion: v1
kind: ConfigMap
metadata:
  name: tractionbuild-config
  namespace: tractionbuild
data:
  # Application configuration
  NEO4J_URI: "neo4j://tractionbuild-neo4j:7687"
  NEO4J_USER: "neo4j"
  PROMETHEUS_PORT: "8000"
  MEMORY_FILE_PATH: "/app/output/project_memory.json"
  CREWAI_MEMORY_PATH: "/app/output/crewai_memory"
  HOME: "/app"
  
  # Logging configuration
  LOG_LEVEL: "INFO"
  CREWAI_LOG_LEVEL: "INFO"
  
  # Environment configuration
  ENVIRONMENT: "production"
  PROJECT_NAME: "tractionbuild"
