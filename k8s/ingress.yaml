apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tractionbuild-ingress
  namespace: tractionbuild
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/rate-limit: "100"
    nginx.ingress.kubernetes.io/rate-limit-window: "1m"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - tractionbuild.local  # Change this to your actual domain
    secretName: tractionbuild-tls
  rules:
  - host: tractionbuild.local  # Change this to your actual domain
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: tractionbuild-app
            port:
              number: 8000
