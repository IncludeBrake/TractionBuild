# ZeroToShip Docker Production Requirements - Minimal Essential Dependencies
# Only includes dependencies actually used in the codebase

# Core AI and Agent Framework
crewai>=0.28.0
langchain>=0.1.0
langchain-openai>=0.0.5
langchain-anthropic>=0.0.5
langchain-community>=0.0.10

# Core Dependencies (actually used)
python-dotenv>=1.0.0
pyyaml>=6.0
requests>=2.31.0
pydantic>=2.0.0
neo4j>=5.0.0

# Web Framework and API (actually used)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# CLI and Utilities (actually used)
rich>=13.0.0
typer>=0.9.0
tenacity>=8.2.0

# Data Processing (actually used)
numpy>=1.24.0
dpath>=2.1.0

# Monitoring (actually used)
prometheus-client>=0.17.0
structlog>=23.0.0

# Security (actually used)
cryptography>=41.0.0

# Production Server (actually used)
gunicorn>=21.0.0

# AWS Services (actually used)
boto3>=1.40.0
botocore>=1.40.0
jmespath>=1.0.1
s3transfer>=0.13.0

# Additional Tools (actually used)
mermaid-cli>=0.1.2
graphviz>=0.20.0

# Environment-specific (actually used)
python-multipart>=0.0.6

# Optional: Uncomment for distributed execution
# redis>=5.0.0
# celery>=5.3.0
