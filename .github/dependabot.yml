version: 2
updates:
  - package-ecosystem: "pip"
    directory: "/"
    schedule: { interval: "weekly" }
    ignore:
      - dependency-name: "*"  # Manual review for critical deps
        update-types: ["version-update:semver-major"]
